# Slack App
SLACK_BOT_TOKEN=xoxb-your-bot-token-here
SLACK_SIGNING_SECRET=your-signing-secret-here

# Slack Channel IDs
SLACK_HR_CHANNEL=C0XXXXXXXX
SLACK_GENERAL_CHANNEL=C0XXXXXXXX

# HR User (for DM confirmations)
HR_EMAIL=pooja.soni@devxlabs.ai

# Google Sheets
GOOGLE_SHEET_ID=your-google-sheet-id
GOOGLE_SERVICE_ACCOUNT_KEY=base64-encoded-service-account-json

# Webhook secret (shared with Apps Script)
WEBHOOK_SECRET=a-random-secret-string

# Vercel Cron Secret (auto-set by Vercel)
CRON_SECRET=your-cron-secret

# Neon Postgres (provision via Vercel Marketplace)
DATABASE_URL=postgresql://user:password@ep-example-123.us-east-1.aws.neon.tech/onboarding?sslmode=require

# SMTP Email (Resend SMTP or any SMTP provider)
SMTP_HOST=smtp.resend.com
SMTP_PORT=465
SMTP_USER=resend
SMTP_PASSWORD=re_your_api_key_here
SMTP_FROM_EMAIL=onboarding@devxlabs.ai
DRY_RUN_EMAIL=hr-test@devxlabs.ai

# Company config is in src/company-config.js (not env vars)

# Server
PORT=3000
